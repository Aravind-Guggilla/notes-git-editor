import "./Toolbar.css";

function Toolbar() {
  return (
    <div className="toolbar">
      <button>New</button>
      <button>Save</button>
      <button>Rename</button>
      <button>Delete</button>
    </div>
  );
}

export default Toolbar;